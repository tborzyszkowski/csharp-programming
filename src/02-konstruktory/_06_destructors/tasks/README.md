# Zadania - Destruktory

## 📝 Zadanie 1: FileLogger z IDisposable

Stwórz klasę `FileLogger` implementującą `IDisposable` do czyszczenia pliku logu.

## 📝 Zadanie 2: Resource Pool

Stwórz `ResourcePool` zarządzający kolekcją zasobów, gdzie `Dispose()` czysczy wszystkie zasoby.

---

## ✅ Rozwiązania w `code/Program.cs`

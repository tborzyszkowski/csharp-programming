# Zadania - Nullable Reference Types

## 📝 Zadanie 1: Person z Nullable Properties

Stwórz `Person` z:
- `Name` (non-nullable)
- `Phone` (nullable)
- `Email` (nullable)

Używaj `?? "N/A"` dla wyświetlania.

## 📝 Zadanie 2: Config z Null Checks

Stwórz `DatabaseConfig` gdzie `Password` jest opcjonalny (`string?`).

## 📝 Zadanie 3: Nullable Value Types

Stwórz `Article` z `PublishedDate` (nullable `DateTime?`) - sprawdzaj `HasValue`.

---

## ✅ Rozwiązania w `code/Program.cs`
